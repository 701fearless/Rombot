# User floorplan data

Runtime copies of customized floorplans are written to
`<user-id>/floorplans/<scene-id>_custom.json` when a scene is saved.

The JSON files are local user data and are ignored by Git. The current demo uses
the `demo-user` identity until account authentication is connected.
