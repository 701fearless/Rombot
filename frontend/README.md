# Frontend

前端已统一为 Vite + React 18 + TypeScript + React Router + Three.js，不再支持 Taro 或微信小程序。

项目背景、完整演示链路、SceneSnapshot 协议、Conda `ml2025` 约束和启动命令统一见仓库根目录的 `README.md`。

```powershell
npm ci --legacy-peer-deps
npm run dev
npm test
npm run typecheck
npm run lint
npm run build
npm run test:e2e
```
