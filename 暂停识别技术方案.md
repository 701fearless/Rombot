# 离线预处理版暂停家具 Tag 技术实现文档

## Summary

将当前“暂停时实时识别”改为“视频入库时离线预处理，暂停时秒查结果”。核心目标是让 H5 在用户暂停视频后 100ms 级返回家具 tag，而不是等待 Grounded-SAM-2 / SAM2 / Meshy 实时推理。

系统拆成两条链路：

```text
离线预处理链路：视频/关键帧 → 检测分割 → crop/mask/tag → analysis.json
在线交互链路：videoId + time → 查最近预处理帧 → 返回 tag → 点击 tag → 建模/商品/空间页
```

当前 FastAPI 后端已有 `detect/select-object/run-pipeline` 基础框架；下一步新增视频预处理模块和基于 `analysis.json` 的快速查询逻辑。

## Key Changes

新增离线预处理能力：

```http
POST /api/video/preprocess
```

请求：

```json
{
  "videoId": "living_room_001",
  "videoUrl": "/sample_data/videos/living_room_001.mp4",
  "sampleIntervalSec": 1.0,
  "mode": "mock|grounded_sam2"
}
```

处理流程：

```text
1. 用 ffmpeg 每 1 秒抽一帧
2. 对每帧调用 Grounded-SAM-2 Lite 服务：Grounding DINO + SAM2.1 tiny
3. 返回 label/confidence/bbox/mask
4. 后端保存 frame/crop/mask
5. 英文 label 转中文 tag
6. 生成 outputs/videos/<videoId>/analysis.json
```

生成文件结构：

```text
outputs/videos/living_room_001/
  analysis.json
  frames/
    000001.jpg
    000002.jpg
  objects/
    frame_000001_obj_sofa_001_crop.jpg
    frame_000001_obj_sofa_001_mask.png
```

`analysis.json` 最小结构：

```json
{
  "videoId": "living_room_001",
  "status": "succeeded",
  "sampleIntervalSec": 1.0,
  "frames": [
    {
      "frameId": "living_room_001_000001",
      "time": 12.0,
      "frameImageUrl": "/outputs/videos/living_room_001/frames/000001.jpg",
      "objects": [
        {
          "id": "obj_sofa_001",
          "label": "sofa",
          "name": "沙发",
          "confidence": 0.91,
          "bbox": [118, 420, 690, 850],
          "tagPosition": [0.43, 0.61],
          "cropUrl": "/outputs/videos/living_room_001/objects/frame_000001_obj_sofa_001_crop.jpg",
          "maskUrl": "/outputs/videos/living_room_001/objects/frame_000001_obj_sofa_001_mask.png"
        }
      ]
    }
  ]
}
```

改造暂停查询接口：

```http
POST /api/feed/detect
```

优先逻辑：

```text
如果存在 outputs/videos/<videoId>/analysis.json：
  读取 analysis.json
  找到最接近 time 的 frame
  返回该 frame 的 objects
否则：
  走当前 mock / grounded_sam2 实时检测 provider
```

用户点击 tag 后：

```http
POST /api/feed/select-object
```

逻辑：

```text
1. 根据 frameId/objectId 从 analysis.json 或 detection.json 读取 object
2. 如果 object 已有 cropUrl/maskUrl，直接复用
3. 用户点击“放进我的小屋”时，再调用 Meshy/Pixal3D 生成 GLB
4. 没有 3D API Key 时返回 mock GLB，保证 H5 演示不断
```

Grounded-SAM-2 Lite 云服务契约保持：

```http
POST /detect-and-segment
```

请求：

```json
{
  "image": "data:image/jpeg;base64,...",
  "prompt": "sofa . bed . chair . coffee table . chandelier . rug . curtain . plant .",
  "box_threshold": 0.35,
  "text_threshold": 0.35,
  "max_objects": 8
}
```

返回：

```json
{
  "objects": [
    {
      "label": "sofa",
      "confidence": 0.91,
      "bbox": [118, 420, 690, 850],
      "mask": "data:image/png;base64,..."
    }
  ]
}
```

## Implementation Changes

在后端新增模块：

```text
app/routers/video.py
app/services/video_preprocess/
  extractor.py
  analysis_store.py
  preprocessor.py
```

职责划分：

```text
extractor.py：调用 ffmpeg 抽帧
analysis_store.py：读写 analysis.json，按 time 查最近帧
preprocessor.py：编排抽帧、检测分割、保存 crop/mask、生成 analysis
video.py：提供 /api/video/preprocess 和 /api/video/analysis/{videoId}
```

新增接口：

```http
GET /api/video/analysis/{videoId}
```

返回指定视频的 `analysis.json`，方便 H5 或调试页查看预处理结果。

```http
GET /api/video/analysis/{videoId}/nearest?time=12.4
```

返回最接近暂停时间的 frame objects，用于调试在线秒查逻辑。

在线 `/api/feed/detect` 的返回格式不变，保证 H5 队友无需大改：

```json
{
  "frameId": "living_room_001_000012",
  "frameImageUrl": "/outputs/videos/living_room_001/frames/000012.jpg",
  "objects": [...]
}
```

预处理模式分三档：

```text
mock：不调用模型，生成固定家具 tag，适合前端联调
grounded_sam2：调用 4090 云端 Grounded-SAM-2 Lite
manual：读取人工校正后的 analysis.json，适合比赛演示兜底
```

推荐黑客松主流程：

```text
1. 准备 5 条家装视频
2. 离线预处理生成 analysis.json
3. 人工快速校正重点时间点的 tag/bbox
4. H5 暂停时只查 analysis.json
5. 用户点 tag 后再触发 Meshy 建模，或直接返回预生成 GLB
```

## GPU Strategy

只租一台 4090 时，云端只跑视觉预处理服务，不跑主后端和 H5。

推荐配置：

```text
GPU：RTX 4090 24GB
CPU 内存：16GB 可跑，24GB 更稳，32GB 最舒服
磁盘：40GB 可跑，60GB 更稳
模型：Grounding DINO 轻量版 + SAM2.1 tiny/small
模式：单图 image-only，不启用 video tracking
输入分辨率：长边 768 或 1024
每帧最多返回：5-8 个 object
```

不建议第一版使用：

```text
SAM2 large
整段视频 tracking
高分辨率原图逐帧推理
暂停瞬间实时识别所有模型
```

## Test Plan

本地后端测试：

```text
1. 用 mock 模式调用 /api/video/preprocess
2. 确认生成 outputs/videos/<videoId>/analysis.json
3. 调 /api/feed/detect，传 videoId + time，不传 frameImage
4. 确认能秒回 objects
5. 调 /api/feed/select-object，确认复用 cropUrl/maskUrl 并返回 glbUrl
```

云 GPU 联调：

```text
1. 启动 Grounded-SAM-2 Lite /detect-and-segment 服务
2. 设置 GROUNDED_SAM2_ENDPOINT
3. 用一条短视频跑 /api/video/preprocess
4. 检查每帧 crop/mask 是否合理
5. 调整 prompt、confidence、max_objects
6. 对 5 条演示视频生成稳定 analysis.json
```

验收标准：

```text
暂停后 /api/feed/detect 平均响应 < 200ms
每条视频至少 3 个关键暂停点有正确家具 tag
tag 位置与家具中心基本一致
点击 tag 后 crop/mask 可查看
点击放进小屋后能返回 GLB 或 mock GLB
Grounded-SAM-2 不可用时，manual/mock analysis 仍可演示
```

## Assumptions

- H5 负责提供 `videoId + currentTime`，不需要暂停时上传图片。
- 预处理阶段可以慢，在线暂停查询必须快。
- Grounded-SAM-2 Lite 只用于离线预处理，不承担在线高并发实时识别。
- Meshy/Pixal3D 仍只负责单个家具 crop 到 GLB，不负责检测和分割。
- 比赛演示允许对样例视频的 `analysis.json` 做人工校正，以保证体验稳定。
